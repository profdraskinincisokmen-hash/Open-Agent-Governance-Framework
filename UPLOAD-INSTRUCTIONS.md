# OAGF Clean Site — Upload Instructions

Replace the current files in your `Open-Agent-Governance-Framework` repository with:

- `index.html`
- `styles.css`
- `script.js`

Keep these existing files in the repository root:

- `logo.png`
- `architecture.png`
- `Open Agent Governance Framework.pdf`

The Google Search Console verification meta tag is already included in `index.html`.
